{
  "name": "harvester",
  "scripts": {
    "ts-check": "bun run --filter '*' ts-check"
  },
  "workspaces": [
    "pkg/runner",
    "pkg/shared"
  ],
  "dependencies": {
    "shared": "workspace:*"
  }
}
